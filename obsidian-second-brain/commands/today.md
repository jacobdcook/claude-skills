# /today — Prioritized day plan from the vault

**Vault:** `YOUR_OBSIDIAN_VAULT` — **`CLAUDE.md`** rules apply.

## Do this

1. If CLI works and daily notes plugin is active: **`obsidian daily:read`** (Obsidian focused on LIFE_WIKI). Else **`rg`**/`Read` Obsidian daily folder (often `Daily/` or `daily/` — discover under vault root).
2. Pull **open tasks**: `- [ ]` in daily note + recent **`wiki/`** pages + **`wiki/career/application-pipeline.md`** if relevant.
3. **Calendar:** Only if ICS/links exist in vault; otherwise say “paste today’s obligations or skip.”
4. Output **one prioritized list**: must-do / should-do / optional, grounded in **what they already marked important this week** in notes.
5. Optional append: **`obsidian daily:append`** with bullets only if CLI works **and** user implied they want tasks written down.
