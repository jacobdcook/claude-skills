# /ideas — Idea report grounded in vault patterns

**Vault:** `YOUR_OBSIDIAN_VAULT` — **`CLAUDE.md`**.

Deep **~30-day** vault pass with **cross-domain** pattern detection and light **graph-style** linking (clusters, hubs) to surface ideas **across all domains** unless scoped.

Optional scope after **`/ideas`** (e.g. “career only”).

## Do this

1. Re-read **`wiki/index.md`** and scan **`wiki/technical/`**, **`wiki/career/`**, **`wiki/concepts/`** for repeated motifs (**`rg`** helps).
2. If CLI works: **`obsidian tags counts`** optionally for signal.
3. Output four buckets (each 3–7 bullets **grounded** in cites): **build**, **people to reach**, **research**, **write**. Label speculation.
4. Do not invent projects that contradict **`wiki/log.md`** priorities unless framed as hypotheses.
