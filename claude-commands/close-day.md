# /close-day — End-of-day processing (alias of /closeday)

**Vault:** `YOUR_OBSIDIAN_VAULT` — **`CLAUDE.md`**.

Same workflow as **`/closeday`**: extract **action items**, surface **vault connections**, check whether **confidence markers** in notes need updates.

## Do this

Follow **`closeday.md`** in the **same commands folder** as this file, step for step. If that file is unavailable, fall back to:

1. Read today’s daily (CLI **`obsidian daily:read`** or glob).
2. Summarize **done / learned / blocker / carry-over** with citations.
3. Propose **`wiki/`** or **`wiki/log.md`** updates for decisions, not only daily blurbs.
4. Persist only with user confirmation.
